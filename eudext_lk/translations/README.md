Add `<lang_code>.csv` here (e.g. `si.csv`, `ta.csv`) with `source_text,translated_text`
rows to translate this app's strings -- standard Frappe translation file
convention, picked up automatically on `bench build`/`bench migrate`.
